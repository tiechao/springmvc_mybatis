<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE configuration  
PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-config.dtd">  
<configuration>  
	<!-- 全量setting配置，根据需要再加 -->

    <!--配置别名  
    typeAliases和environments是平等级别的标签  
        type是实体类的完整类名  
        alias是类的别名  
    -->  
    <typeAliases>  
        <!-- <typeAlias type="com.soft.domain.User" alias="User"/>  
        <typeAlias type="com.soft.domain.DeptEntity" alias="DeptEntity"/>  
        <typeAlias type="com.so ft.domain.EmpEntity" alias="EmpEntity"/>  -->
		<!-- 批量扫描别名-->
		<package neme="cn.ibatis.po" />
    </typeAliases>  
      
 <!--    <environments default="development">  
        <environment id="development">  
            <transactionManager type="JDBC" />  
            <dataSource type="POOLED">  
                <property name="driver" value="com.mysql.jdbc.Driver" />  
                <property name="url" value="jdbc:mysql://localhost/he" />  
                <property name="username" value="root" />  
                <property name="password" value="root" />  
            </dataSource>  
        </environment>  
    </environments>  --> 
   <!--  <mappers>  
         映射文件的位置   
		配置mapper
			由于使用spring和mybatis的整合包进行mapper扫描，这里不需要配置了
			必须遵循：mapper.xml和mapper.java 文件同名 且在一个目录下
		
        <mapper resource="com/soft/domain/User.xml" />  
      
    </mappers>  --> 
</configuration>  